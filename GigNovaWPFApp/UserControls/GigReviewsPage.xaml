<UserControl x:Class="GigNovaWPFApp.UserControls.GigReviewsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="NiceButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#1B1B1B">
        <StackPanel Margin="25" HorizontalAlignment="Center">
            <Border Background="#111111" BorderBrush="#B83C5A" BorderThickness="1" CornerRadius="10" Width="1100" Padding="24">
                <StackPanel>
                    <Border BorderBrush="#3A3A3A" BorderThickness="0,0,0,1" Padding="0,0,0,14" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock x:Name="HeaderText" Text="Gig Reviews (0)" Foreground="White" FontSize="32" FontWeight="Bold"/>
                            <TextBlock x:Name="AverageText" Text="Overall average rating: 0.0" Foreground="#BFBFBF" FontSize="15" FontWeight="SemiBold" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>

                    <StackPanel x:Name="ReviewsList"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>